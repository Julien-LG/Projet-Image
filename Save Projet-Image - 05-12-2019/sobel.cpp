#include <stdexcept>
/** @file
 * Filtres de Sobel
 **/
#include <cmath>
#include "sobel.h"

ImageGris intensiteH(ImageGris img) {
    // Remplacer la ligne suivante par le code adéquat
    throw runtime_error("Fonction intensiteH non implanté ligne 10");
}

ImageGris intensiteV(ImageGris img) {
    // Remplacer la ligne suivante par le code adéquat
    throw runtime_error("Fonction intensiteV non implanté ligne 15");
}

ImageGris intensite(ImageGris img) {
    // Remplacer la ligne suivante par le code adéquat
    throw runtime_error("Fonction intensite non implanté ligne 20");
}
