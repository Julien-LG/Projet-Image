#include <stdexcept>
/** @file
 * Lecture et ecriture d'images en niveau de gris au format PGM
 **/

#include <iostream>
#include <fstream>
#include "image.h"
#include "pgm.h"



ImageGris lirePGM(string source) {
    // Remplacer la ligne suivante par le code adéquat
    throw runtime_error("Fonction lirePGM non implanté ligne 15");
}

void ecrirePGM(ImageGris img, string cible) {
    // Remplacer la ligne suivante par le code adéquat
    throw runtime_error("Fonction ecrirePGM non implanté ligne 20");
}

bool ImageGrisEgal(ImageGris a, ImageGris b, float precision) {
    // Remplacer la ligne suivante par le code adéquat
    throw runtime_error("Fonction ImageGrisEgal non implanté ligne 25");
}

ImageGris inversePGM(ImageGris img) {
    // Remplacer la ligne suivante par le code adéquat
    throw runtime_error("Fonction inversePGM non implanté ligne 30");
}
